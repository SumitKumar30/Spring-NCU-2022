package org.ncu.springcoupling;

public class FactoryGreetingService {
	/*
	 * based on the language a person speaks - it'll create object of that
	 * dependency class
	 */
	
	/*
	 * public GreetingService getGreetingService(String lang) {
	 * if(lang.equals("english")) { return new EnglishGreetingService(); }else
	 * if(lang.equals("french")) { return new FrenchGreetingService(); }else
	 * if(lang.equals("spanish")) { return new SpanishGreetingService(); }else {
	 * throw new
	 * RuntimeException("No greeting service exists for "+lang+" language"); } }
	 */
}
